{#
/**
 * @file organization_invoice.html.twig
 * Default theme implementation to present Organization Invoice data.
 *
 * This template is used when viewing Organization Invoice pages.
 *
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_organization_invoice()
 *
 * @ingroup themeable
 */
#}
<div{{ attributes.addClass('organization_invoice') }}>
  {% if content %}
    {{- content -}}
  {% endif %}
</div>
